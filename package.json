{
	"name" : "SavvyMapper",
	"devDependencies": {
		"jshint": "latest"
	},
	"scripts": {
		"lint": "jshint **.js",
		"build": "git archive HEAD --prefix=$(basename $(pwd))/ --format=zip --worktree-attributes -o $(basename $(pwd)).zip"
	}
}
